## Основные правила для всего:
## !!!СТРОГО!!!
## БЕЗ КОМАНДЫ НЕ РАБОТАЕМ!

Сначала размышляем вместе. 
Работаем только по командам:
- "Делай"
- "Согласен" (на твой вопрос: "Если хочешь, я могу...").
Юмор приветствуется в любом виде. 
Меня тоже можно и нужно подкалывать, с самооценкой у меня всё в порядке. ;)

## Репозитории:
- Можешь всмё коммитить и пушить сам и сразу, усли что: откатим, главное не испортить репу. 
- Проверки все на мне, не запускаешь ничего без согласования, ты мне нужен не для таких тупых задач, кат отладка. ))) 

## Новая цель проекта

Берём за основу то, чего уже добились, НО!
Наш билдер работает по оригу pkg-oss (https://github.com/nginx/pkg-oss).
Т.е. в корне pkg-oss есть файлик build_module.sh.
В нём есть все необходимые для сборки кастомных модулей, надо лишь указать Url с исходниками, имя, версию исходников,
бранч pkg-oss ну и там по мелочи ещё... 
Вот наша задача чекать обновления и запускать билдеры по необходимости или оповещать в телеграм. Это уже есть, 
но надо разнести по операцонкам, в том плане, чтобы я мог отдельно выбрать, какие собираем сразу при выходе обновления,
а какие просто получаю уведомления и запускаю руками если и когда надо.

Соответственно: у нас уже есть свои образы билдеров под centos 10 и almalinux 9, вот именно ими уже клонируем туда репу pkg-oss
задаём в inputs workflow модули и ветку (stable, mainline или all) и запускаем билдер. 
Либо лучше работать с базовыми образами, т.к. pkg-oss, по идее, должен сам подтянуть зависимости.
Я проверил на markdown-filter под нашим centos 10, скрипт проверил мои репы, установил зависимости и всё быстренько собрал.

### Ответы на вопросы

1. На твоё усмотрение, но там в pkg-oss уже есть свой генератор. Может юзать его при каждой сборке?
   Просто я не совсем понимаю, что он делает. Возможно в pkg-oss уже лежат готовые матрицы. 
   Наша цель собрать по их фен-шую, добавив свои модули и конфиги немного изменив или дополнив их маршруты сборки.
2. Наша основная цель, собрать репу для каждой ОС в итоге. Но сейчас главное Centos 10 и Almalinux.
   По rpm я уже дальше сам разберусь может. Ну или попрошу тебя по мелочи.
3. Наборы модулей будут как сейчас в check-version.yml, другое дело, что их надо упорядочить по базовым, динамическим и отдельно те
   модули, которые будем добавлять кастомно. У них массив параметров для скрипта build_module.sh, поэтому наверное надо как-то оформлять
   подключамыми файлами. Я пока не понимаю как лучше, чтобы не перегружать workflow inputs или workflow global env.
4. Не совсем понял вопроса. Наша задача собрать репы для обновления и добавить свои модули. В pkg-oss, версии разбиты на бранчи. 
   Мы можем тащить сразу нужный бранч с нужной версией, которая по идее уже должна собираться как нам надо. 
   В конце просто прогоняем скриптик build_module.sh с нашими модулями и кладём всё это по нашим репам.
5. Сообщения по обновлению самой версии nginx, далее как указано в workflow input или global env для каждой ОС.
6. Пока только телеграм.
7. Из п.п. 3. Разбивка по базовым, динамическим они в inputs, а вот кастомные модули можно и в global env, предложи как. ))
   Дело в том, что для rpm например, прямо на входе в Makefile можно перечислить модули, которые он уже и сам умеет собирать.
   Ну и пусть собирает именно так, мы всегда можем использовать код pkg-oss для правильной сборки. Свои модули собираем их же скриптом,
   который тоже желательно не модифицировать, ибо он может измениться. Проще поменять ему входные параметры, чем править при каждом изменении.
8. Надо посмотреть, что сделает сам pkg-oss, если его сунуть в голый (почти, git clone то нам нужен) образ. Мне понравилось, как отработал скрипт build_module.sh.
   Он всё проверил, подтянул зависимости и всё собрал очень быстро. Он там тянет какой то свой набор для пакаджей.
9. Мне нет. )
10. Их по месту, пока не критично. 
